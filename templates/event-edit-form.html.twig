{#
/**
 * @file
 * Default theme implementation for a event edit form.
 *
 * Two column template for the event add/edit form.
 *
 * This template will be used when a event edit form specifies 'event_edit_form'
 * as its #theme callback.  Otherwise, by default, event add/edit forms will be
 * themed by form.html.twig.
 *
 * Available variables:
 * - form: The event add/edit form.
 *
 * @see seven_form_event_form_alter()
 *
 * @ingroup themeable
 */
#}
{{ form }}
